/**
 * Clone of framer-motion's Reorder component
 * But uses 'm' instead of 'motion', so it can be LazyLoaded
 */

import { Group } from "./Group";
import { Item } from "./Item";

export const Reorder = {
	Group,
	Item,
};
